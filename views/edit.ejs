<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Book Notes | Jonathan Afugwobi</title>
        <link rel="stylesheet" href="/styles/style.css">
        <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    </head>
    <body>

        <div class="main">
            <div class="container">
                <div class="edit">
                    <% if (locals.item) { %> 
                        <h2>Edit Book Details</h2>
            
                        <form action="/edit-book/<%= item[0].id %>" method="POST" id="edit-form">
                            <input type="text" name="editTitle" value="<%= item[0].title %>" placeholder="Title" required>
                            <input type="number" name="editIsbn" value="<%= item[0].isbn %>" placeholder="ISBN" required>
                            <input type="text" id="dateInput" name="editDate" value="<%= item[0].date %>" pattern="\d{4}\/(0[1-9]|1[0-2])\/(0[1-9]|[12][0-9]|3[01])" placeholder="YYYY/MM/DD" required>
                            <input type="text" name="editRating" maxlength="2" value="<%= item[0].rating %>" placeholder="Rate over 10" required>
                            <textarea name="editSummary" id="summary" rows="5" placeholder="Book Summary" required><%= item[0].summary %></textarea>
                            <textarea name="editNote" id="note" placeholder="Your Note" rows="10"><%= item[0].note %></textarea>
            
                            <div class="edit-btns">
                                <button type="submit">Update</button>
                                <a href="/">Cancel</a>
                            </div>
                        </form>
                    <% } %>
            
                    <% if (locals.error) { %> 
                        <p class="error"> <%= error %> </p>
                    <% } %> 
                </div>
            </div>
        </div>
    
        <footer>
            <p>Copyright &copy; <%= new Date().getFullYear() %> - Jonathan Afugwobi || All rights reserved.</p>
        </footer>

        <script src="/js/main.js"></script>
    </body>
</html>